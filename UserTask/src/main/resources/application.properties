# Application Configuration
spring.application.name=UserTask
server.port=8080

# Database Configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://usertask-db.clq0o0suglp1.ap-south-1.rds.amazonaws.com:3306/testdb?createDatabaseIfNotExist=true&useSSL=true&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME:admin}
spring.datasource.password=${DB_PASSWORD:Taskdb123}

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.generate-ddl=true
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Web Configuration
spring.mvc.pathmatch.matching-strategy=ANT_PATH_MATCHER
spring.main.allow-circular-references=true

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:lasanthachulabhaya@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:nihm vgjp cgob qhwn}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000

# Password Reset Configuration
app.password-reset.token-validity-minutes=15
app.password-reset.base-url=${FRONTEND_URL:http://task-mgtsystem-frontend.s3-website.ap-south-1.amazonaws.com}

# JWT Configuration
jwt.secret=${JWT_SECRET:LasanthaChulabaya546}
jwt.expiration=86400

# Production Optimizations
spring.jpa.show-sql=false
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=INFO